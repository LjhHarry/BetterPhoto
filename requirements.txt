# ============================================================
# PSR-Net 实验依赖
# ============================================================
# 安装: pip install -r requirements.txt
#
# 云端 GPU Notebook 额外安装 (B1/B2 完整模式):
#   pip install diffusers transformers accelerate
# ============================================================

# ── 核心依赖（所有实验必需）──────────────────────────────────
torch>=2.0.0
numpy>=1.21.0
matplotlib>=3.5.0
Pillow>=9.0.0
adjust-text>=1.1.1
scipy>=1.7.0

# ── 可选：B1/B2 风格精修 & 高清增强（SD v1.5 完整模式）──────
# diffusers>=0.25.0
# transformers>=4.36.0
# accelerate>=0.25.0
